version: '3.8'

services:
  web:
    image: node:20-alpine
    working_dir: /app
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    command: npm run dev
    environment:
      - WATCHPACK_POLLING=true